import './App.css';
import Home from './components/home/home';

function App() {
  return (
    <Home/>
  );
}

export default App;

/**
 * TODO: 
 * 
 * User can load a text file of coordinate points that they want to create a GOL instance of.
 * 
 * User can then load these from localstorage whenever they feel like it.
 * 
 * User can update the color of the cells.
 * 
 * User can save the current configuration that they're in
 * 
 * ETA of finishing: 1 week deadline?
 */